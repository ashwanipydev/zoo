import { Component } from '@angular/core';

@Component({
  selector: 'app-gatekeeper-page',
  standalone: true,
  template: `
    <section class="page page-step">
      <h1>Gatekeeper Dashboard</h1>
      <p>Scan and validate visitor tickets at the gate.</p>
    </section>
  `
})
export class GatekeeperPage {}
